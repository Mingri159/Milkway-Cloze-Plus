// 方向键 前进后退 步数
var step = 5;

// 生词弹窗、右侧单词释义列表  右键单击时  文中的等级标注  是否取消
var is_mark = true;

// 选择高等级时， 低等级的也同时自动标注
var mark_too = false;

// 提示 弹窗
window.QMSG_GLOBALS = {
  DEFAULTS: {
    showClose: false,
    timeout: 2000,
  },
};
